import { Helmet } from "react-helmet-async";

const ContactUs = () => {
    return (
        <section>
            <Helmet>
                <title>Bistro Boss || Contact</title>
            </Helmet>

        </section>
    );
};

export default ContactUs;