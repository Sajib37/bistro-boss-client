import { Helmet } from "react-helmet-async";

const Dashboard = () => {
    return (
        <section>
            <Helmet>
                <title>Bistro Boss || Dashboard</title>
            </Helmet>
        </section>
    );
};

export default Dashboard;